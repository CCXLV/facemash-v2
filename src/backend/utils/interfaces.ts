export interface ImageDataFetch {
    id: number;
    name: string;
    rating: number;
    data: string;
};

export interface ImageRatingFetch {
    id: number;
    name: string;
    rating: number;
    unique_id: string;
};